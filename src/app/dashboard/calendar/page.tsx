import { ComingSoon } from "../_components/ComingSoon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Deadline Calendar"
      description="A monthly calendar view of all your application deadlines, RFI response windows, and decision dates — colour coded by urgency."
    />
  );
}
