import { ComingSoon } from "../_components/ComingSoon";

export default function TimelinePage() {
  return (
    <ComingSoon
      title="Application Timeline"
      description="A visual timeline showing the progress of each application through the planning stages — from submission to decision."
    />
  );
}
