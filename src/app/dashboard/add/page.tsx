import { ComingSoon } from "../_components/ComingSoon";

export default function AddApplicationPage() {
  return (
    <ComingSoon
      title="Add Application"
      description="Add a new planning application to your pipeline. Track reference numbers, deadlines, client details, and status updates all in one place."
    />
  );
}
